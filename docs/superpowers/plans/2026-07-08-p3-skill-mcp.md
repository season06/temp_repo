# P3 — Skill & MCP Integration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 讓 SDK 能以累加式 `AgentBuilder` 掛載多個 MCP server(stdio + HTTP/SSE)與多個 skill(mock registry),把它們轉成 tools 注入原生 DeepAgent。

**Architecture:** 採 **Builder** 形狀:`AgentBuilder(config).add_mcp(...).add_skill(...).add_hook(...).build()` → 回傳**原生** agent(內部委派 P1/P2 的 `factory.build_agent`)。MCP 走 **langchain-mcp-adapters** 的 `MultiServerMCPClient`(原生支援 stdio/HTTP、把 MCP tools 轉 langchain BaseTool);細節隔離在 `agent_template/mcp.py`。skill 走可替換的 `SkillRegistry`(MVP 用 `MockSkillRegistry`),`skill_to_tool` 把 skill 轉成一個 tool;隔離在 `agent_template/skills.py`。

**Tech Stack:** langchain-mcp-adapters 0.3.0（`MultiServerMCPClient`）、mcp 1.28.1（FastMCP,測試用 server）、langchain_core.tools（`StructuredTool`）、deepagents 0.6.12、pytest。

## Global Constraints

- **前置**：P0/P1/P2 完成（`factory.build_agent(config, hooks=None)`、`HookMiddleware` 皆就緒）。
- **新相依（已安裝,務必 pin)**：`langchain-mcp-adapters==0.3.0`（連帶 `mcp==1.28.1`）。已於 `.venv`;本 phase 只需在 `pyproject.toml` 補上這條相依,**不需**再 install。
- **已驗證的 langchain-mcp-adapters 事實（地基,勿臆測）**：
  - `MultiServerMCPClient(connections)`,`connections` 是 `dict[server_name -> connection dict]`。
  - `client.get_tools()` 是 **async**,回傳 `list[BaseTool]`。
  - connection 形狀：stdio = `{"transport": "stdio", "command": <exe>, "args": [<script>...]}`;HTTP = `{"transport": "streamable_http", "url": "http://host/mcp"}`。
  - **MCP 產生的 tool 是 async-only**：同步 `tool.invoke(...)` 會 `NotImplementedError`;要用 `tool.ainvoke(...)`（於同步碼/測試中以 `asyncio.run(...)` 包）。
  - 在**同步**環境中 `asyncio.run(client.get_tools())` 可正常運作（無執行中的 event loop）。
- **skill → tool（已驗證)**：`StructuredTool.from_function(func=<無參數 func>, name=..., description=...)`;`tool.invoke({})` 回傳該 func 的回傳值（我方 skill tool 的 func 是同步,支援 sync invoke）。
- **Builder 回傳原生物件**：`build()` 委派 `factory.build_agent`,回傳原生 DeepAgent。
- **型別註記慣例**：只標註 dict/list;不 import `typing`;不標註 primitive;plain class + `__init__`。
- **執行測試**：`.venv/bin/python -m pytest`（repo 根目錄）。整合測試會 spawn 子行程跑 MCP server(用 `sys.executable`),屬預期。
- **Commit 時機**：commit 為執行期動作,由使用者決定何時執行本計畫。

---

### Task 1: MCP tool 載入（`mcp.py`）+ 測試用 MCP server

**Files:**
- Create: `agent_template/mcp.py`
- Create: `tests/mcp_server.py`（FastMCP stdio server fixture)
- Modify: `pyproject.toml`（補相依）
- Test: `tests/test_mcp.py`

**Interfaces:**
- Consumes: `langchain_mcp_adapters.client.MultiServerMCPClient`。
- Produces: `agent_template.mcp.load_mcp_tools(connections)` — 同步回傳所有 MCP server 的 `list[BaseTool]`;`connections` 空（`None`/`{}`)回傳 `[]`。模組層級須有可被 monkeypatch 的名稱 `MultiServerMCPClient`。

- [ ] **Step 1: 補 `pyproject.toml` 相依**

Edit `pyproject.toml` 的 `dependencies`,加入一行（保持既有兩條）：

```toml
dependencies = [
    "deepagents==0.6.12",
    "langchain-openai==1.3.3",
    "langchain-mcp-adapters==0.3.0",
]
```

Verify 可 import：`.venv/bin/python -c "import langchain_mcp_adapters; print('ok')"` → 應印 `ok`。

- [ ] **Step 2: 建立測試用 MCP server**

Create `tests/mcp_server.py`:

```python
from mcp.server.fastmcp import FastMCP

mcp = FastMCP("testserver")


@mcp.tool()
def echo(text: str) -> str:
    """echoes text back"""
    return "echo:" + text


if __name__ == "__main__":
    mcp.run(transport="stdio")
```

- [ ] **Step 3: 寫測試（會失敗）**

Create `tests/test_mcp.py`:

```python
import asyncio
import os
import sys

from agent_template import mcp as mcp_mod
from agent_template.mcp import load_mcp_tools


def test_empty_connections_returns_empty():
    assert load_mcp_tools({}) == []
    assert load_mcp_tools(None) == []


def test_load_returns_client_tools(monkeypatch):
    class FakeClient:
        def __init__(self, connections):
            self.connections = connections

        async def get_tools(self):
            return ["TOOL_A", "TOOL_B"]

    monkeypatch.setattr(mcp_mod, "MultiServerMCPClient", FakeClient)
    tools = load_mcp_tools({"s": {"transport": "stdio"}})
    assert tools == ["TOOL_A", "TOOL_B"]


def test_load_real_stdio_server():
    server = os.path.join(os.path.dirname(__file__), "mcp_server.py")
    tools = load_mcp_tools({
        "test": {"transport": "stdio", "command": sys.executable, "args": [server]}
    })
    names = [t.name for t in tools]
    assert "echo" in names
    echo = next(t for t in tools if t.name == "echo")
    # MCP tools are async-only; invoke via ainvoke
    result = asyncio.run(echo.ainvoke({"text": "hi"}))
    assert "echo:hi" in str(result)
```

- [ ] **Step 4: 跑測試確認失敗**

Run: `.venv/bin/python -m pytest tests/test_mcp.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'agent_template.mcp'`。

- [ ] **Step 5: 實作 `mcp.py`**

Create `agent_template/mcp.py`:

```python
import asyncio

from langchain_mcp_adapters.client import MultiServerMCPClient


def load_mcp_tools(connections):
    """同步載入所有 MCP server 的 tools。

    connections: dict[server_name -> connection dict]，例如：
      stdio: {"transport": "stdio", "command": "python", "args": ["server.py"]}
      http:  {"transport": "streamable_http", "url": "http://host/mcp"}
    回傳 langchain BaseTool 清單;connections 為空則回傳 []。
    MCP tool 為 async-only（呼叫端需用 ainvoke）。
    """
    if not connections:
        return []

    async def _load():
        client = MultiServerMCPClient(connections)
        return await client.get_tools()

    return asyncio.run(_load())
```

- [ ] **Step 6: 跑測試確認通過**

Run: `.venv/bin/python -m pytest tests/test_mcp.py -v`
Expected: PASS（3 passed;第三項會 spawn 子行程 MCP server,稍慢屬正常）。

- [ ] **Step 7: Commit**

```bash
cd /home/xizhen/文件/temp_repo
git add pyproject.toml agent_template/mcp.py tests/mcp_server.py tests/test_mcp.py
git commit -m "feat: add load_mcp_tools via MultiServerMCPClient (stdio + http)"
```

---

### Task 2: Skill registry 與 skill→tool（`skills.py`）

**Files:**
- Create: `agent_template/skills.py`
- Test: `tests/test_skills.py`

**Interfaces:**
- Consumes: `langchain_core.tools.StructuredTool`。
- Produces:
  - `Skill(name, description, content)` — 同名屬性。
  - `SkillRegistry` — 介面,`get(skill_id)` 預設 `raise NotImplementedError`。
  - `MockSkillRegistry(skills=None)` — `skills` 為 `dict[skill_id -> Skill]`;`get(skill_id)` 回傳對應 Skill(找不到丟 `KeyError`)。
  - `skill_to_tool(skill)` — 回傳一個 `StructuredTool`(無參數,`invoke({})` 回傳 `skill.content`),名稱/描述取自 skill。

- [ ] **Step 1: 寫測試（會失敗）**

Create `tests/test_skills.py`:

```python
import pytest

from agent_template.skills import Skill, SkillRegistry, MockSkillRegistry, skill_to_tool


def test_mock_registry_get_returns_skill():
    s = Skill(name="greet", description="d", content="hello")
    reg = MockSkillRegistry({"greet": s})
    assert reg.get("greet") is s


def test_mock_registry_unknown_raises():
    reg = MockSkillRegistry({})
    with pytest.raises(KeyError):
        reg.get("nope")


def test_base_registry_not_implemented():
    with pytest.raises(NotImplementedError):
        SkillRegistry().get("x")


def test_skill_to_tool_produces_callable_tool():
    s = Skill(name="greet", description="returns a greeting", content="hi from skill")
    tool = skill_to_tool(s)
    assert tool.name == "greet"
    assert tool.description == "returns a greeting"
    assert tool.invoke({}) == "hi from skill"
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `.venv/bin/python -m pytest tests/test_skills.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'agent_template.skills'`。

- [ ] **Step 3: 實作 `skills.py`**

Create `agent_template/skills.py`:

```python
from langchain_core.tools import StructuredTool


class Skill:
    """從 skill-registry 取得的 skill（MVP mock 形狀:名稱 + 描述 + 內容）。"""

    def __init__(self, name, description, content):
        self.name = name
        self.description = description
        self.content = content


class SkillRegistry:
    """skill 來源介面。正式 registry 由團隊敲定後實作;MVP 用 MockSkillRegistry。"""

    def get(self, skill_id):
        raise NotImplementedError


class MockSkillRegistry(SkillRegistry):
    def __init__(self, skills=None):
        # skills: dict[skill_id -> Skill]
        self._skills = skills or {}

    def get(self, skill_id):
        return self._skills[skill_id]


def skill_to_tool(skill):
    """把一個 Skill 轉成可被 agent 呼叫的 tool（MVP:無參數,回傳 skill.content）。"""

    def _run():
        return skill.content

    return StructuredTool.from_function(func=_run, name=skill.name, description=skill.description)
```

- [ ] **Step 4: 跑測試確認通過**

Run: `.venv/bin/python -m pytest tests/test_skills.py -v`
Expected: PASS（4 passed）。

- [ ] **Step 5: Commit**

```bash
cd /home/xizhen/文件/temp_repo
git add agent_template/skills.py tests/test_skills.py
git commit -m "feat: add SkillRegistry (mock) and skill_to_tool"
```

---

### Task 3: `build_agent` 接受 tools

**Files:**
- Modify: `agent_template/factory.py`
- Test: `tests/test_factory.py`（同檔追加）

**Interfaces:**
- Consumes: 無新增。
- Produces: `build_agent(config, hooks=None, tools=None)` — 新增 `tools` 參數,傳給 `create_deep_agent(tools=tools or [])`。與現有呼叫(P1/P2）相容。

- [ ] **Step 1: 追加測試（會失敗）**

Append to `tests/test_factory.py`:

```python
def test_build_agent_passes_tools(monkeypatch):
    calls = {}
    monkeypatch.setattr(factory, "ChatOpenAI", lambda **k: "LLM")
    monkeypatch.setattr(factory, "create_deep_agent", lambda **k: calls.update(k) or "AGENT")
    cfg = AgentConfig(api_key="k", base_url="b", model="m")
    factory.build_agent(cfg, tools=["T1", "T2"])
    assert calls["tools"] == ["T1", "T2"]


def test_build_agent_defaults_tools_to_empty(monkeypatch):
    calls = {}
    monkeypatch.setattr(factory, "ChatOpenAI", lambda **k: "LLM")
    monkeypatch.setattr(factory, "create_deep_agent", lambda **k: calls.update(k) or "AGENT")
    factory.build_agent(AgentConfig(api_key="k", base_url="b", model="m"))
    assert calls["tools"] == []
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `.venv/bin/python -m pytest tests/test_factory.py::test_build_agent_passes_tools -v`
Expected: FAIL — `build_agent()` 目前不接受 `tools`（TypeError）。

- [ ] **Step 3: 修改 `build_agent`**

Edit `agent_template/factory.py` 的 `build_agent`（保留 hooks 邏輯,新增 tools）：

```python
def build_agent(config, hooks=None, tools=None):
    """建構並回傳原生 DeepAgent 物件（執行期回歸原生,無包裝）。
    hooks: Hook 清單;tools: 額外 tools（如 MCP / skill 轉出的 tool）。"""
    llm = _build_llm(config)
    middleware = [HookMiddleware(hooks)] if hooks else []
    return create_deep_agent(
        model=llm,
        tools=tools or [],
        system_prompt=config.system_prompt,
        middleware=middleware,
    )
```

- [ ] **Step 4: 跑測試確認通過**

Run: `.venv/bin/python -m pytest tests/test_factory.py -v`
Expected: PASS（P1/P2 既有 5 項 + 本 Task 2 項 = 7 passed;既有測試仍綠 = 向後相容）。

- [ ] **Step 5: Commit**

```bash
cd /home/xizhen/文件/temp_repo
git add agent_template/factory.py tests/test_factory.py
git commit -m "feat: build_agent accepts tools param"
```

---

### Task 4: `AgentBuilder`（累加式 add_mcp / add_skill / add_hook / build）

**Files:**
- Create: `agent_template/builder.py`
- Test: `tests/test_builder.py`

**Interfaces:**
- Consumes: `agent_template.factory.build_agent`、`agent_template.mcp.load_mcp_tools`、`agent_template.skills.MockSkillRegistry` / `skill_to_tool`、`agent_template.hooks.Hook`。
- Produces: `AgentBuilder(config, skill_registry=None)`:
  - `add_mcp(name, connection)` → 累加到 `self._mcp_connections`;回傳 `self`。
  - `add_skill(skill_id)` → 累加到 `self._skill_ids`;回傳 `self`。
  - `add_hook(hook)` → 累加到 `self._hooks`;回傳 `self`。
  - `build()` → `tools = load_mcp_tools(connections) + [skill_to_tool(registry.get(id)) for id ...]`;委派 `build_agent(config, hooks=self._hooks, tools=tools)`;回傳原生 agent。
  - 模組層級須有可被 monkeypatch 的名稱 `load_mcp_tools` 與 `build_agent`。

- [ ] **Step 1: 寫測試（會失敗）**

Create `tests/test_builder.py`:

```python
import os
import sys

import agent_template.builder as bmod
import agent_template.factory as factory
from agent_template.builder import AgentBuilder
from agent_template.config import AgentConfig
from agent_template.hooks import Hook
from agent_template.skills import Skill, MockSkillRegistry


def _cfg():
    return AgentConfig(api_key="k", base_url="b", model="m")


def test_add_methods_chain_and_accumulate():
    b = AgentBuilder(_cfg())
    r = b.add_mcp("s1", {"transport": "stdio"}).add_skill("greet").add_hook(Hook())
    assert r is b
    assert b._mcp_connections == {"s1": {"transport": "stdio"}}
    assert b._skill_ids == ["greet"]
    assert len(b._hooks) == 1


def test_build_combines_mcp_and_skill_tools(monkeypatch):
    monkeypatch.setattr(bmod, "load_mcp_tools", lambda conns: ["MCP_TOOL"] if conns else [])
    captured = {}
    monkeypatch.setattr(bmod, "build_agent",
                        lambda config, hooks, tools: captured.update(hooks=hooks, tools=tools) or "AGENT")
    reg = MockSkillRegistry({"greet": Skill("greet", "d", "hi")})
    b = AgentBuilder(_cfg(), skill_registry=reg)
    agent = b.add_mcp("s1", {"transport": "stdio"}).add_skill("greet").build()
    assert agent == "AGENT"
    tools = captured["tools"]
    assert "MCP_TOOL" in tools
    assert any(getattr(t, "name", None) == "greet" for t in tools)


def test_build_wires_real_mcp_and_skill_tools_into_create_deep_agent(monkeypatch):
    captured = {}
    monkeypatch.setattr(factory, "ChatOpenAI", lambda **k: "LLM")
    monkeypatch.setattr(factory, "create_deep_agent", lambda **k: captured.update(k) or "AGENT")
    server = os.path.join(os.path.dirname(__file__), "mcp_server.py")
    reg = MockSkillRegistry({"greet": Skill("greet", "d", "hi from skill")})
    b = AgentBuilder(_cfg(), skill_registry=reg)
    b.add_mcp("test", {"transport": "stdio", "command": sys.executable, "args": [server]}).add_skill("greet")
    agent = b.build()
    assert agent == "AGENT"
    names = [getattr(t, "name", None) for t in captured["tools"]]
    assert "echo" in names   # real MCP tool loaded from the subprocess server
    assert "greet" in names  # skill tool
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `.venv/bin/python -m pytest tests/test_builder.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'agent_template.builder'`。

- [ ] **Step 3: 實作 `builder.py`**

Create `agent_template/builder.py`:

```python
from .factory import build_agent
from .mcp import load_mcp_tools
from .skills import MockSkillRegistry, skill_to_tool


class AgentBuilder:
    """累加式建構器:add_mcp / add_skill / add_hook,最後 build() 回傳原生 agent。"""

    def __init__(self, config, skill_registry=None):
        self._config = config
        self._registry = skill_registry or MockSkillRegistry()
        self._mcp_connections = {}
        self._skill_ids = []
        self._hooks = []

    def add_mcp(self, name, connection):
        self._mcp_connections[name] = connection
        return self

    def add_skill(self, skill_id):
        self._skill_ids.append(skill_id)
        return self

    def add_hook(self, hook):
        self._hooks.append(hook)
        return self

    def build(self):
        tools = list(load_mcp_tools(self._mcp_connections))
        for skill_id in self._skill_ids:
            tools.append(skill_to_tool(self._registry.get(skill_id)))
        return build_agent(self._config, hooks=self._hooks, tools=tools)
```

- [ ] **Step 4: 跑測試確認通過**

Run: `.venv/bin/python -m pytest tests/test_builder.py -v`
Expected: PASS（3 passed;第三項會 spawn 子行程 MCP server)。

- [ ] **Step 5: 跑全部測試確認整體綠**

Run: `.venv/bin/python -m pytest -q`
Expected: PASS（P0/P1/P2 的 33 + P3:mcp 3 + skills 4 + factory 2 + builder 3 = 45 passed）。

- [ ] **Step 6: Commit**

```bash
cd /home/xizhen/文件/temp_repo
git add agent_template/builder.py tests/test_builder.py
git commit -m "feat: add AgentBuilder with add_mcp/add_skill/add_hook/build"
```

---

## Self-Review

- **Spec coverage**（req.md Feature 4）：
  - 「add_mcp / add_skill 擴充能力」→ `AgentBuilder.add_mcp` / `add_skill`（可重複呼叫累加）✅
  - 「mcp client:連線、生命週期、tool 轉換」→ `load_mcp_tools` 用 `MultiServerMCPClient`（stdio + HTTP/SSE),每次 `get_tools` 自管連線 ✅
  - 「mcp 兩種 transport（stdio + HTTP/SSE)」→ connection dict `transport` 欄支援 `stdio` / `streamable_http`（測試以 stdio 實跑,HTTP 形狀已文件化）✅
  - 「多個 mcp / 多個 skill」→ `_mcp_connections` dict、`_skill_ids` list 累加 ✅
  - 「skill client:從 skill-registry 拿 content(未定義,可 mock）」→ `SkillRegistry` 介面 + `MockSkillRegistry`;`skill_to_tool` 轉 tool（skill → tool 決策）✅
  - 架構:Builder 回傳原生物件;MCP/skill 細節各自隔離在 `mcp.py` / `skills.py` ✅
  - 與 P2 相容:`build()` 帶入 hooks（`add_hook`）→ `build_agent(config, hooks, tools)` ✅
- **Placeholder scan**：無 TBD/TODO;每步含完整、已對真實庫實測的程式碼（MCP async-only、asyncio.run、StructuredTool、connection 形狀均已 spike 驗證）。✅
- **Type consistency**：`load_mcp_tools(connections)`、`AgentBuilder(config, skill_registry=None)` 的 `add_mcp(name, connection)`/`add_skill(skill_id)`/`add_hook(hook)`/`build()`、`build_agent(config, hooks=None, tools=None)`、`Skill(name, description, content)`、`skill_to_tool(skill)` 在各 Task 與測試一致;builder 以關鍵字 `hooks=`/`tools=` 呼叫 build_agent,與其簽章相符。✅
- **Scope**：auth 打 HTTP 是 P4、o11y 是 P5 —— 本 phase 不碰。skill content 的正式格式團隊未定,MVP 以「無參數回傳 content 的 tool」佔位,`SkillRegistry` 介面可換。✅
