agent:
  # name: my-agent   # 填了且設 AGENT_REGISTRY_URL 才會啟用 agent registry
  provider: deepagent
  model: qwen-max
  system_prompt: "you are a helpful agent"

# MCP 需要可連線的 server 才能啟用,範例以註解呈現
# local_mcp:
#   - name: my-mcp
#     transport: streamable-http
#     url: http://localhost:8000/mcp

local_tools:
  - "./tools/"

local_skills:
  - "./skills/"

# 遠端 A2A agent(build 時抓對方 card,包成 tool 給 LLM 用)
# local_a2a:
#   - name: research-agent
#     url: http://other-host:9000

# agent_card: "./agent_card.yaml"   # card 檔路徑;不寫預設找同目錄的 agent_card.yaml
