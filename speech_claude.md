## Slide 1 — 封面 / 標題頁（約 30 秒）
投影片內容:「監控系統現代化 — 從 SQL 到可觀測性平台」
逐字稿:

各位好,今天我要分享的是我們團隊過去這段時間在監控系統上做的一些演進。

我們的產品線同時涵蓋 iSOP 和 TX 兩個系統,如何確保這兩個系統的健康狀態能被有效監控,一直是維運上的核心課題。今天會從現況的背景出發,帶大家看我們做了什麼、解決了什麼問題,以及接下來要走的方向。

## Slide 2 — Background:現有監控的局限（約 1.5 分鐘）
投影片內容:兩欄式,左側「One Defense 的價值」,右側「遇到的瓶頸」
逐字稿:

我們組織目前有一套既有的監控平台叫做 One Defense。它的運作原理是透過定期執行 SQL Query,把資料庫裡的業務數據轉換成監控指標,對我們的日常維運有一定的幫助。
但隨著系統複雜度提升,我們發現 One Defense 有幾個根本上無法突破的限制:第一,SQL 能查到的只有「資料庫裡的資料」,對於系統的執行狀態、排程健康度、容器資源這類維運層面的指標,它完全看不到。第二,當系統出問題,工程師要查案,只能靠零散的文字 log,缺乏上下文,非常耗時。
所以我們決定在 One Defense 之外,自建一套可觀測性平台來補足這個缺口。

## Slide 3 — Architecture:監控平台架構（約 1.5 分鐘）
投影片內容:架構示意圖,兩層:自建 Stack（Prometheus / AlertManager / Grafana / Elasticsearch / Kibana） + One Defense
逐字稿:

這是我們目前的監控架構全貌。
下層是組織提供的 One Defense,繼續負責 SQL 層級的業務指標監控,這部分我們保留並沿用。
上層是我們自建的可觀測性平台,核心組件有五個:Prometheus 負責抓取和儲存系統指標;AlertManager 負責告警的路由與通知;Grafana 是統一的視覺化儀表板;Elasticsearch 負責收集和儲存 log;Kibana 則是 log 的查詢介面。
這套架構同時服務 iSOP 和 TX 兩個產品線,讓我們在一個平台上就能掌握兩個系統的健康狀態。

## Slide 4 — 痛點與優化(三項成果)（約 2.5 分鐘）
投影片內容:三張卡片式佈局,每張對應一項優化,標題+成效數字
逐字稿:

建立這套平台的過程中,我們也遭遇了不少維運上的痛點,並且逐一做出優化。這裡跟大家報告三個最關鍵的改善。
第一個是 Grafana 集中化。 過去我們在 14 個不同的環境裡各自跑一座 Grafana,工程師查問題時要在多個介面之間切換,維護成本也非常高。我們現在把所有環境的資料來源都集中到一座 Grafana,工程師只需要在同一個地方就能切換查看不同環境的狀態。這大幅降低了切換成本,也讓儀表板的維護從 14 份變成 1 份。
第二個是 Log 結構化。 原本系統吐出來的 log 是純文字,格式不一致,工程師要從一大串文字裡找出問題根因,非常費時。我們將 log 統一轉換成 JSON 格式,每一筆 log 都帶有明確的欄位和上下文資訊,查案時可以直接過濾、搜尋,大幅縮短排查時間。
第三個是指標覆蓋補強。 我們針對幾個 SQL 無法監控到的關鍵系統,增加了專屬的指標。包含 Airflow 的排程健康度、Conductor 的工作流執行狀態,以及 Kubernetes 叢集的資源使用情況。這些指標讓我們能在問題發生之前就提前預警,而不是等到系統掛掉才被動處理。

## Slide 5 — Future Work:下一步規劃（約 1.5 分鐘）
投影片內容:兩個方向,各有 icon + 標題 + 一句說明
逐字稿:

最後跟大家分享兩個正在規劃中的方向。
第一個是 SRE Agent,結合 AI 提升查案效率。 目前工程師收到告警後,仍然需要手動去 Grafana 和 Kibana 裡翻找資訊、交叉比對。我們計畫引入 AI Agent 來協助這個流程:當告警觸發時,Agent 能自動分析相關的指標和 log,提供初步的診斷報告,讓工程師能更快速地定位問題。
第二個是 Grafana Sanity Check 自動截圖。 每次系統部署或重大變更後,我們都需要人工確認各項指標是否正常。我們計畫開發自動化機制,在關鍵時間點自動截取 Grafana 儀表板的畫面,並直接回報到通訊頻道,讓驗證流程從原本需要人工操作縮短為自動完成。

## Slide 6 — 總結（約 30 秒）
投影片內容:三行成果摘要 + 一句方向性結語
逐字稿:

總結一下今天分享的內容。
我們在既有的 One Defense 基礎上,自建了一套涵蓋 iSOP 和 TX 的可觀測性平台。透過 Grafana 集中化、Log 結構化、以及指標覆蓋補強三項優化,我們顯著降低了維運成本和查案時間。接下來我們會進一步引入 AI 能力,讓監控從被動告警走向主動診斷。
以上是本次的報告,謝謝大家,如果有任何問題歡迎提問。
