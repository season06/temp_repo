# SDK 程式碼保護與打包方案決策

**日期**：2026-08-17
**對象**：`agent-template` SDK
**目的**：對外散布時提高竄改難度、避免使用者看到內部原始碼，並選出成本最低、可跨平台的做法。

---

## TL;DR

散布 `agent-template` 時**只出 `.pyc`（sourceless），wheel 內不含任何 `.py`**，用 `-OO` 編譯以剝除 docstring／assert；公開 API 另附 `.pyi` stub 保留下游開發體驗。產出 **3 顆 OS 無關的 wheel**（Python 3.11／3.12／3.13 各一）。

**不採用編譯（Nuitka/Cython）**，因為它的保護強度超出實際需求，卻要付「每個 OS × 每個 Python 版本各編一份」的跨平台成本；**也不採用純源碼混淆**，因為它輸出的仍是 `.py`，無法達成「看不到原始碼」。

---

## 1. 需求與威脅模型

| 項目 | 內容 |
|---|---|
| 保護目標 | (a) 增加 SDK 內部程式碼**被竄改的難度**；(b) **避免使用者看到原始碼** |
| 使用者 | **內部部門開發者**（半信任對象，非外部不可信對象） |
| 硬性限制 | **免費**（不採付費工具）、**跨 Linux／Windows**、**低環境建置成本** |

威脅模型的關鍵：對象是內部半信任開發者，目的是「提高門檻、避免隨手看／隨手改」，**不是**防禦有決心的專業逆向工程。這條界線決定了「不需要為最強保護付出最高成本」。

### 一個重要的範圍釐清：runtime 覆蓋擋不住

「防竄改」有兩種：

- **(a) 改安裝檔**：開發者打開 `site-packages/...` 直接改內部 `.py`。→ **打包手段可以處理**。
- **(b) runtime 猴補丁**：在自己程式裡 `import` 後重新指派內部函式／屬性來覆蓋行為。→ **任何打包／混淆／編譯都擋不住**，這是 Python 動態語言的天性，只能靠程式碼層加固（final class、name-mangling、唯讀屬性等）緩解，且無法根絕。

本決策的範圍是 **(a)**。若日後要處理 (b)，屬於程式碼設計議題，與打包無關，另案處理。

---

## 2. 方法比較

| 方法 | 本質 | 增加竄改難度 | 避免看到原始碼 | 跨 Linux/Windows | 需編譯器/headers | 免費 |
|---|---|---|---|---|---|---|
| 純源碼混淆<br>(python-minifier 等) | 改名、壓平，**輸出仍是 `.py`** | 低（檔案可直接改） | ❌ **未達成**，源碼照在 | 一顆通用 | 否 | ✔ |
| **只出 `.pyc`（本案採用）** | 丟掉 `.py`，只給 bytecode | 中（需反編譯後重編） | ⚠️ 大致達成（可被反編譯還原近似源碼） | **OS 無關**，只綁 Python 次版 | **否** | ✔ |
| 編譯 `.so`/`.pyd`<br>(Nuitka/Cython) | 編成原生機器碼 | 高（不能改） | ✅ 達成 | **每 OS × 每 Python 版各編一份** | **是** | ✔ |
| PyArmor | 加密 bytecode + runtime 解密 | 中 | ⚠️ | 需 runtime | 否 | ✖ 付費 |

補充說明：

- **Cython 與 Nuitka 同屬「編譯成原生碼」**，因此想避開的跨平台編譯成本，Cython 一樣有，並非「源碼混淆」的替代品。
- **純源碼混淆無法滿足「避免看到原始碼」**——輸出還是可讀的 `.py`，只是變醜，開發者照樣讀得懂邏輯。這一條直接把它淘汰。
- **PyArmor 需付費**，違反硬性限制，不列入。

---

## 3. 最終選擇與理由

**選擇：只出 `.pyc`（sourceless）+ `-OO` 編譯 + 公開 API `.pyi` stub。**

理由是它精準命中需求與限制的交集：

1. **達成「避免看到原始碼」**：wheel 內零 `.py`，內部開發者打開只見 bytecode，不會有人順手去反編譯。
2. **達成「增加竄改難度」**：不能直接改檔，要動手得先反編譯、改、再重編，門檻明顯提高；`-OO` 再剝掉 docstring／assert，進一步降低可讀性。
3. **成本最低、跨平台**：`.pyc` bytecode **與 OS 無關**，同一顆在 Linux／Windows 都能跑，**只綁 Python 次版**。因此免編譯器、免 `Python.h`、免 manylinux、免 Windows 工具鏈——完全避開編譯方案的環境地獄。代價僅是「每個支援的 Python 次版各出一顆 wheel」。
4. **保留下游開發體驗**：只出 `.pyc` 會讓 IDE／mypy 讀不到型別。因此對公開 API（`AgentBuilder`、`Config`、`ConfigError`）附 `.pyi` stub + `py.typed`，開發者對公開介面仍有自動補全與型別檢查；stub 只描述公開簽名，不含實作，不洩漏內部。

**為何不選編譯（`.so`/`.pyd`）**：它能提供「真的無法還原」的最強保護，但那是為了防禦專業逆向——超出「內部半信任開發者」的實際威脅。換取這點強度要付出「OS × Python 版本」的建置矩陣與 C 工具鏈維護成本，成本效益不划算。

---

## 4. 已知並接受的代價

| 代價 | 說明 | 為何可接受 |
|---|---|---|
| `.pyc` 可被反編譯 | `decompyle3`／`uncompyle` 可還原成近似源碼 | 威脅模型只需擋「隨手看／隨手改」，不需擋決心逆向者 |
| traceback 無源碼行 | 下游錯誤堆疊有行號但不顯示源碼內容 | debug 體驗略降，內部可接受 |
| 綁 Python 次版 | 每新增一個 Python 次版（如 3.14）要再編一顆 wheel | 目前只需 3.11–3.13，維護量可控 |
| runtime 覆蓋擋不住 | 見 §1 範圍釐清 | 屬程式碼設計議題，不在打包範圍 |

---

## 5. 實作摘要

- **產出**：`agent_template-<版本>-cp311-none-any.whl`、`cp312`、`cp313` 共 3 顆，放 `dist/`。
- **編譯**：以 uv-managed Python（3.11／3.12／3.13）逐版，用 `-OO` 把 `agent_template/**/*.py` 編成 sourceless 的 `foo.pyc`（放在模組路徑上，非 `__pycache__`）。
- **打包**：hatchling build hook（`hatch_build.py`）`force_include` 這些 `.pyc`、排除所有 `.py`、將 wheel tag 設為 `cpXY-none-any`。設定寫進 `pyproject.toml`，SDK 作者本地 editable 開發不受影響。
- **stub**：公開 API 出 `.pyi` + `py.typed`，隨 wheel 一起散布。
- **驗收**：每顆在對應 Python 的乾淨 venv `pip install` 後，確認可 `import`、安裝目錄內**只有 `.pyc`/`.pyi`/`py.typed`、無任何 `.py`**，並通過現有測試。

---

## 附錄：決策脈絡一覽

1. 威脅模型＝內部半信任開發者、目的是提高門檻 → 不需最強保護 → 排除高成本編譯路線。
2. 硬限制免費 → 排除 PyArmor。
3. 需求含「避免看到原始碼」 → 排除純源碼混淆（源碼仍在）。
4. 剩下「只出 `.pyc`」命中所有條件 → 定案，並補 `.pyi` stub 補回開發體驗。

---

## Relate Work

「防止 override」具體是指哪一種？

┌───────────────┬────────────────────────────────────────────────────────────────┬─────────────────────────────────────────────────────────────────────────────────────────────────┐
│     情境      │                        開發者實際做的事                        │                                       打包/混淆擋得住嗎？                                       │
├───────────────┼────────────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────┤
│ (a) 改安裝檔  │ 打開 site-packages/agent_template/xxx.py 直接改內部邏輯        │ ✅ 擋得住——不出 .py（出 .pyc 或改到很難讀）就大幅提高門檻                                       │
├───────────────┼────────────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────┤
│ (b) runtime   │ import agent_template; agent_template.core.factory.xxx =       │ ❌ 完全擋不住——這是 Python 動態語言的天性，混淆/編譯都沒用，只能靠程式碼層防護（final           │
│ 猴補丁        │ my_func 在自己程式裡覆蓋                                       │ class、__setattr__ 鎖、私有命名）解                                                             │
└───────────────┴────────────────────────────────────────────────────────────────┴─────────────────────────────────────────────────────────────────────────────────────────────────┘

ByteCode & OS 
┌───────────────────────┬────────────────────────────────────────────────────────┬──────────────────────────────────────────────────────────────────────────────────┐
│                       │                     現行方案 .pyc                      │                                編譯 .so（Nuitka）                                │
├───────────────────────┼────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────┤
│ 檔案內容              │ CPython bytecode（直譯器的原生指令）                   │ 原生機器碼（x86/ARM 的 CPU 指令）                                                │
├───────────────────────┼────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────┤
│ 產生方式              │ py_compile 把源碼轉成 bytecode                         │ 把 Python 翻成 C，再用 C 編譯器編成機器碼                                        │
├───────────────────────┼────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────┤
│ 需要 CPython？        │ 要——因為 bytecode 就是「給 CPython VM 吃的語言」       │ 也要——但理由不同：它是個「呼叫 CPython C-API 的擴充模組」，借用 CPython 的引擎跑 │
├───────────────────────┼────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────┤
│ 可還原性              │ 高：bytecode 幾乎 1:1 對應源碼結構，可反編譯回近似 .py │ 低：源碼結構被展開成機器碼，沒有成熟的反編譯器                                   │
├───────────────────────┼────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────┤
│ 綁定                  │ 只綁 Python 次版，跨 OS/CPU                            │ 綁 OS × CPU 架構 × Python 版本                                                   │
├───────────────────────┼────────────────────────────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────┤
│ 需要 C 編譯器/headers │ 否                                                     │ 是                                                                               │
└───────────────────────┴────────────────────────────────────────────────────────┴──────────────────────────────────────────────────────────────────────────────────┘

## runtime 猴補丁 

拿你 builder.py 裡最典型的例子：_merge_middlewares 註解明寫「SDK 必要的那組不可移除」。這正是一段「你想強制、但 runtime 擋不住」的內部邏輯。

原本寫法

```
# builder.py（模組層）
_SDK_MIDDLEWARE = [audit_mw, safety_mw]          # 假設之後填了必要 middleware

def _merge_middlewares(sdk_items, build_items):
    return [*sdk_items, *build_items]            # SDK 在前，保證掛上

class AgentBuilder:
    def build(self, **kwargs):
        ...
        middleware = _merge_middlewares(_SDK_MIDDLEWARE, kwargs.pop("middleware", None) or [])
```

部門開發者的 runtime 補丁（三行就繞掉，且不改任何安裝檔）

```
import agent_template.core.builder as b

# 手法1：換掉合併函式，直接丟掉 SDK 那組
b._merge_middlewares = lambda sdk, user: list(user)

# 手法2：把來源清單清空
b._SDK_MIDDLEWARE.clear()

# 之後照常用，SDK 強制 middleware 就沒了
agent = b.AgentBuilder("config.yaml").build()
```

會成功的原因：build() 在呼叫當下才去模組全域查 _merge_middlewares 和 _SDK_MID派，就換掉了行為。混淆或編譯成 .so 都擋不住這招——b.xxx = ...對編譯過的模組一樣有效。

如何修改讓這招失效（提高門檻，非絕對）

核心原則：別讓內部呼叫「經過一個可被外部重新指派的名字」。把值在 import 時就綁進閉包/區域，並用 name-mangling + 唯讀屬性把它藏起來：

```
class AgentBuilder:
    # __sdk_mw 會被改名成 _AgentBuilder__sdk_mw，外部不易命中
    __sdk_mw = (audit_mw, safety_mw)             # tuple 不可變，clear() 無效

    def build(self, **kwargs):
        user_mw = kwargs.pop("middleware", None) or []
        # 直接讀 class 私有屬性、就地合併，不經任何模組全域函式
        middleware = [*type(self).__sdk_mw, *user_mw]
        ...
```

這樣：
- 沒有 _merge_middlewares 模組函式可換 → 手法1失效；
- __sdk_mw 是 tuple → 手法2的 .clear() 失效；
- 名字被 mangling 藏起來 → 要改得先知道 _AgentBuilder__sdk_mw，門檻變高。

但務必誠實：天花板就在這。 開發者仍可 AgentBuilder.build = my_build 整個換掉方法，或 sys.modules 抽換。Python 是動態語言，runtime 覆蓋沒有任何打包手段能根絕，只能「一層層加摩擦」。

---

直接回答：「只做源碼層混淆、不編譯」無法同時滿足你這兩個需求——它卡在「避免開發者看到原始碼」這一條。

原因很簡單：源碼層混淆（python-minifier 那類）輸出的還是 .py，只是變得又醜又難讀。開發者打開 site-packages 照樣看得到原始碼、照樣讀得懂邏輯，只是煩一點。所以：

┌────────────────────┬──────────────────────┬───────────────────────────────────────────┬──────────────────────────┐
│        需求        │ 源碼混淆（仍是 .py） │           只出 .pyc（丟掉 .py）           │      編譯 .so/.pyd       │
├────────────────────┼──────────────────────┼───────────────────────────────────────────┼──────────────────────────┤
│ 增加竄改難度       │ 低（檔案還能直接改） │ 中（要先反編譯再重編 bytecode）           │ 高（不能改）             │
├────────────────────┼──────────────────────┼───────────────────────────────────────────┼──────────────────────────┤
│ 避免看到原始碼     │ ❌ 沒達成，源碼照在  │ ⚠️ 大致達成，但 decompyle3 可還原近似源碼 │ ✅ 達成                  │
├────────────────────┼──────────────────────┼───────────────────────────────────────────┼──────────────────────────┤
│ 跨 Linux/Windows   │ 一顆通用             │ OS 無關，只綁 Python 次版                 │ 每個 OS×次版各編一份     │
├────────────────────┼──────────────────────┼───────────────────────────────────────────┼──────────────────────────┤
│ 需要編譯器/headers │ 否                   │ 否                                        │ 是（你想避開的環境地獄） │
└────────────────────┴──────────────────────┴───────────────────────────────────────────┴──────────────────────────┘

.pyc 有個你會喜歡的性質：bytecode 跟 OS 無關——同一顆 .pyc 在 Linux 和 Windows 都能跑，它只綁「Python 次版」（3.14 的 .pyc 不能在 3.13 用）。所以：

- 達成「看不到源碼」：套件裡完全沒有 .py，內部開發者打開只看到 bytecode，不會有人順手去反編譯 → 對半信任的部門對象夠了。
- 達成「增加竄改難度」：不能直接改檔，要動手得先反編譯、改、再重編，門檻明顯提高。
- 成本最低：免編譯器、免 Python.h、免 manylinux、免 Windows 工具鏈；一個 OS 無關的 wheel，只要「每個支援的 Python 次版各出一顆」。

誠實的天花板：.pyc 可被 decompyle3/uncompyle 還原成近似源碼。所以它擋得住「隨手看、隨手改」的內部開發者，擋不住「決心反編譯」的人。但你的威脅模型（內部部門開發者、增加難度）本來就不需要擋後者——要擋後者才需要付編譯的代價。
