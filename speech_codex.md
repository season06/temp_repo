# Monitor Stack 維運優化：逐頁講稿

> 對象：管理層  
> 總時長：約 9 分 30 秒  
> 原則：未提供量化成效，因此講稿不使用未驗證數字。

## 第 1 頁｜封面：自建 Monitor Stack 維運優化

**建議時間：30 秒**

各位主管好，今天想說明團隊如何建立並優化監控平台，讓 isop 與 tx 兩個產品的維運更可視、問題更容易定位，也讓團隊能把更多時間放在改善服務，而不是反覆切換系統與人工查案。

這次的核心不只是導入幾套工具，而是建立一個能持續擴充、降低維運風險的監控能力。

## 第 2 頁｜背景：既有監控能力與限制

**建議時間：1 分鐘**

組織原本已經有 One Defense 監控系統，可以透過 SQL query 把資料轉換成監控指標。這套機制對資料庫層面的檢查很有價值，也是既有防線的重要一環。

但隨著系統服務化、容器化，單靠 SQL 能看到的範圍開始不足。像是服務執行狀態、工作排程、Kubernetes 資源使用量，或應用程式內部的異常，往往不是單一 SQL query 可以完整呈現。

因此，我們不是取代 One Defense，而是補足它的觀測缺口，逐步將適合的監控項目轉移到 Prometheus 架構，形成更完整的監控能力。

## 第 3 頁｜架構：雙軌監控、整合管理

**建議時間：1 分 15 秒**

目前架構是雙軌並行。

第一軌是組織既有的 One Defense，負責從 SQL query 取得資料並轉換為監控指標，延續既有流程與既有保障。

第二軌是團隊自建的 monitor stack。Prometheus 負責收集 metrics，AlertManager 負責告警整合與派送，Grafana 提供可視化儀表板；同時，我們透過 Elasticsearch 與 Kibana 集中管理日誌。

這樣的設計可同時支援 isop 與 tx，不需要因為產品不同而建立各自獨立、難以維護的監控方式。

對管理面來說，這代表監控能力不再依賴單一工具或單一資料型態，而是能依問題類型選擇最合適的觀測方式。

## 第 4 頁｜痛點與優化一：Grafana 集中化

**建議時間：1 分 30 秒**

第一個改善是 Grafana 集中化。

過去團隊需要面對 14 個環境，也就是可能有 14 套 Grafana 或多個不同入口。維運人員在查案時，必須知道問題位於哪個環境、切換到正確的 Grafana，再確認對應的 dashboard。這不只增加操作成本，也會拉長事件處理時間。

我們將各環境的資料來源集中管理，只保留一座 Grafana 作為統一入口。

這項改善帶來三個直接效益：第一，降低 Grafana 本身的維護與升級成本；第二，減少查案時切換平台的時間；第三，讓 dashboard 的使用方式更一致，新成員也更容易上手。

重點是，我們把原本分散的觀測入口集中起來，讓資訊取得更快、更一致。

## 第 5 頁｜痛點與優化二：日誌結構化

**建議時間：1 分 15 秒**

第二個改善是日誌結構化。

過去許多 log 是 plain text。當事件發生時，工程師需要從大量文字中拼湊時間、請求內容、服務名稱與錯誤原因，資訊不一定完整，也不容易精準搜尋。

我們將 log 逐步轉為 JSON 格式，讓每筆紀錄能帶有一致的上下文，例如服務、環境、請求識別、錯誤類型與時間資訊。

這使日誌不再只是事後閱讀的文字，而是可被搜尋、篩選與關聯分析的資料。

對維運來說，查案時可以更快從症狀找到受影響的服務與請求；對管理面來說，這代表事件處理較少依賴個人經驗，知識能夠被系統化保留。

## 第 6 頁｜痛點與優化三：補足 Metrics 覆蓋

**建議時間：1 分 30 秒**

第三個改善是增加系統層與服務層的 metrics。

SQL 監控適合回答資料面的問題，例如某項資料是否異常、某個數量是否超過門檻；但它難以即時反映服務是否健康、排程是否堆積，或 Kubernetes 資源是否出現瓶頸。

因此，我們針對不同系統補上可觀測的 metrics，包含 Airflow 的工作排程、Conductor 的流程執行狀態，以及 Kubernetes 的資源與工作負載指標。

這些 metrics 讓團隊可以更早發現問題。例如在使用者受影響之前，先看到排程延遲、工作堆積或資源異常，再透過告警與 dashboard 進行處置。

這項投資的核心價值是把維運從「故障後查原因」，逐步轉為「在影響擴大前發現風險」。

## 第 7 頁｜未來工作：AI 輔助與驗證自動化

**建議時間：1 分 30 秒**

下一步，我們會著重兩個方向。

第一，是結合 AI 提升查案效率，也就是 SRE Agent。未來的目標不是讓 AI 取代工程師判斷，而是讓它協助彙整告警、metrics 與 logs，快速提供初步脈絡與可能的排查方向。

這可以降低工程師在多套系統之間手動找資料的時間，讓人力更集中在判斷、決策與真正需要經驗的問題上。

第二，是 Sanity Check 自動化。當系統變更或事件處理完成後，我們希望能自動擷取對應的 Grafana Panel，快速確認關鍵指標是否恢復正常，並提供可追溯的驗證結果。

這能讓回覆更快，也讓每次變更後的驗證流程更一致。

## 第 8 頁｜結語：從工具建置走向穩定營運能力

**建議時間：1 分鐘**

總結來說，團隊目前的方向是保留 One Defense 在 SQL 監控上的價值，同時以 Prometheus-based monitor stack 補足服務、流程與基礎設施的可觀測性。

已完成的優化包含集中 Grafana、結構化日誌，以及擴大 metrics 覆蓋範圍。這些工作共同降低了維運複雜度，也讓問題定位更有效率。

下一階段將以 AI 輔助查案與自動化驗證為重點，持續把監控從被動告警，提升為更主動、更可追溯的營運能力。

以上是今天的說明，謝謝。
